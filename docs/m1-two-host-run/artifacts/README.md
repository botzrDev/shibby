# Artifacts (HLX-109)

**Empty on purpose until a real two-host run.**

After the recorded run, add at least:

- `caller-callrecord.json`
- `callee-callrecord.json`
- `dialer-stdout.txt` (must include `rtt_ms=` and `outcome=`)
- `notes.md` (networks, non-loopback peer/addrs; no reliability claims)

Do not commit synthetic or loopback-only records as if they closed the ticket.
